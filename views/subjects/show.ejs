<div class="container-fluid">
    <h1><%= subject.title %></h1>
    <hr>
    <% if(tutorships.length <= 0){ %>
        <span class="h3">Aun no hay asesorias para esta materia.</span>
    <% } %>
    <% for(tutorship of tutorships){ %>
        <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 p-2">
            <div class="card" style="cursor: pointer;">
                <div class="card-body">
                    <h2>¿Quien la ofrece? <%= tutorship.id %></h2>
                    <small class="text-right">Grupo de max. 5 personas</small>
                </div>
                <div class="card-footer">
                    Lugares disponibles: 3
                </div>
            </div>
        </div>
    <% } %>
</div>